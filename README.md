# Employee-Database
Employee Database
• Designed a relational database schema with structured table creation and defined primary keys
• Implemented full CRUD operations — INSERT, SELECT, UPDATE, and DELETE — on structured employee data
• Managed and manipulated records using conditional queries and WHERE clause filtering
